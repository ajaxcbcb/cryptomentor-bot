🚨 CryptoMentor AI: FINAL DEBUG REQUEST

Dear Replit Agent,

The following critical issues in my CryptoMentor AI project must be fixed. This is related to the CoinGlass V4 integration and Telegram Markdown handling:

------------------------------------------------------------
🔧 ISSUE 1: CoinGlass V4 Data Not Realtime or Missing

In `/futures`, `/analyze`, and `/market`, the CoinGlass V4 API either returns:
• Dummy price like $70,000 (not realtime)
• Or shows: "⚠️ Data CoinGlass tidak tersedia sementara"

✅ FIX REQUESTED:
- Make sure the following API headers are used properly:
  - URL: https://open-api-v4.coinglass.com/public/v4/futures
  - Header: X-API-KEY (value from secrets COINGLASS_API_KEY)

- Ensure the following endpoints are used correctly:
  • `/longShortRate` (returns long/short ratio)
  • `/openInterest` (returns open interest)
  • `/fundingRate` (returns funding rate)
  • `/liquidationMap` (returns liquidation zones)

- Symbols should be standard (e.g. BTC, ETH), NOT like "binance_btcusdt".

------------------------------------------------------------
🔧 ISSUE 2: `CryptoAPI` missing methods

The bot shows:
• Error: `'CryptoAPI' object has no attribute 'get_binance_long_short_ratio'`

✅ FIX REQUESTED:
Please define the following functions inside `CryptoAPI` or its helper module:
- get_binance_long_short_ratio(symbol)
- get_binance_open_interest(symbol)
- get_binance_funding_rate(symbol)
- get_liquidation_zones(symbol)

They must use the new CoinGlass V4 data structure and return clean values.

------------------------------------------------------------
🔧 ISSUE 3: Telegram Markdown Error

In `/market` command:
• Error: `Can't parse entities: can't find end of the entity starting at byte offset 116`

✅ FIX REQUESTED:
- Escape MarkdownV2 characters in message output (`*`, `_`, `[`, `]`, `.`, etc.)
- OR fallback to `parse_mode='HTML'` if MarkdownV2 is too strict

------------------------------------------------------------
✅ OBJECTIVE SUMMARY

1. All CoinGlass data must be fetched from V4 in real-time
2. The `/futures`, `/analyze`, and `/market` commands must use it properly
3. Telegram output must be sanitized or sent in HTML if Markdown fails
4. Symbols like BTC must not include "binance_" prefix

Please inject the logic as needed and ensure clean log output after fixes.
